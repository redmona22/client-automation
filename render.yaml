services:
  - type: web
    name: client-automation
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_RED_USERNAME
        value: "admin"
      - key: NODE_RED_PASSWORD
        value: "RADHEkrishna25@@"  # ← Change this!
      - key: NODE_ENV
        value: "production"
